# ============================================================
# FINANCE DASHBOARD — Environment Variables
# Copy this file to .env.local and fill in all values
# ============================================================

# -----------------------------------------------------------
# APP AUTH — Single shared password for the dashboard
# Pick any strong password. This is what you type on login.
# -----------------------------------------------------------
APP_PASSWORD=your_secure_password_here

# Secret used to sign the session cookie. Run this to generate:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=generate_a_32_byte_hex_string_here

# -----------------------------------------------------------
# SUPABASE — Your database
# Get these from: https://supabase.com → Project → Settings → API
# -----------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=eyJ...your_service_role_key_here

# -----------------------------------------------------------
# GOOGLE OAUTH — For Gmail reading only (not app login)
# Create at: https://console.cloud.google.com/
#   1. New project → Enable Gmail API
#   2. OAuth consent screen → External → Add your email as test user
#   3. Credentials → OAuth 2.0 Client ID → Web application
#   4. Authorised redirect URI: http://localhost:3000/api/auth/callback/google
#      (and: https://your-domain.com/api/auth/callback/google for prod)
# -----------------------------------------------------------
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_google_client_secret

# NextAuth secret — generate same way as SESSION_SECRET
NEXTAUTH_SECRET=another_32_byte_hex_string
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------
# DEPLOYMENT (fill in when you deploy to Vercel)
# -----------------------------------------------------------
# NEXTAUTH_URL=https://finance.yourdomain.com

# -----------------------------------------------------------
# GOOGLE SHEETS SYNC — Your deployed Apps Script web app URL
# 1. Open your Google Sheet → Extensions → Apps Script
# 2. Deploy → Manage Deployments → Edit → New version → Deploy
#    (Execute as: Me, Who has access: Anyone)
# 3. Copy the web app URL and paste it below
# -----------------------------------------------------------
SHEETS_WEBAPP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# -----------------------------------------------------------
# GOOGLE GEMINI — AI-powered Smart Picks analysis (free tier)
# Get a free API key from: https://aistudio.google.com/
# Free tier: 1,500 requests/day — plenty for personal use
# -----------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------
# WEEKLY EMAIL DIGEST — Sends a styled weekly financial summary
# Sign up free at: https://resend.com (3,000 emails/month free)
# -----------------------------------------------------------
RESEND_API_KEY=re_your_resend_api_key_here
WEEKLY_DIGEST_TO=you@youremail.com

# Protect the /api/weekly-digest endpoint — set this to any
# random string and use it as the Bearer token in cron-job.org
# Generate one: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
CRON_SECRET=generate_a_random_secret_here
